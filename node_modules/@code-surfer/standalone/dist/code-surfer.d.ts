/// <reference types="react" />
import { Step } from "code-surfer-types";
declare type CodeSurferProps = {
    steps: Step[];
    progress: number;
    tokens: string[][];
    types: string[][];
};
export declare function CodeSurfer({ progress, steps, tokens, types }: CodeSurferProps): JSX.Element;
export {};
