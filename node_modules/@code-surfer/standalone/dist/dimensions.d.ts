import React from "react";
import { Step, Dimensions } from "code-surfer-types";
declare type DimensionsResult = {
    steps?: Step[];
    dimensions?: Dimensions;
};
declare function useDimensions<T extends HTMLElement | null>(ref: React.MutableRefObject<T>, steps: Step[]): DimensionsResult;
export default useDimensions;
