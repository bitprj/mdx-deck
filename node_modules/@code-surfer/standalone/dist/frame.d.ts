/// <reference types="react" />
import { Step, Dimensions } from "code-surfer-types";
declare type ContainerProps = {
    progress: number;
    dimensions?: Dimensions;
    steps: Step[];
    tokens: string[][];
    types: string[][];
};
declare function CodeSurferContainer({ progress, dimensions, steps, tokens, types }: ContainerProps): JSX.Element;
export default CodeSurferContainer;
