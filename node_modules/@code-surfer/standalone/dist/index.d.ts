/// <reference types="react" />
import { InputStep, Step } from "code-surfer-types";
import { CodeSurferTheme } from "@code-surfer/themes";
import { UnknownError } from "./errors";
import "./default-syntaxes";
declare type ParsedSteps = {
    steps: Step[];
    tokens: string[][];
    types: string[][];
};
declare type CodeSurferProps = {
    steps?: InputStep[];
    parsedSteps?: ParsedSteps;
    progress: number;
    theme?: CodeSurferTheme;
    nonblocking?: boolean;
};
declare function CodeSurferWrapper({ theme, nonblocking, ...props }: CodeSurferProps): JSX.Element;
export { CodeSurferWrapper as CodeSurfer, UnknownError };
