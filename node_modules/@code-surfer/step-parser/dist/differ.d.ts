export declare function generateIds(lineIds: number[], afterId: number | undefined, count: number): number[];
export declare function linesDiff(codeList: string[]): {
    lineIds: number[];
    steps: number[][];
};
